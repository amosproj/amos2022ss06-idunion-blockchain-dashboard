version: '3.7'

services:
  grafana:
    image: grafana/grafana-oss:8.5.0

    container_name: grafana
    restart: unless-stopped
    ports:
      - 3000:3000
    volumes:
       - ./data:/var/lib/grafana
