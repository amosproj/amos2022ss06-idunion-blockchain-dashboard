version: "3.7"
services:
  grafana:
    
  prometheus:
    #    network_mode: host
    image: prom/prometheus:v2.35.0
    container_name: prometheus
    ports:
      - 9090:9090
    volumes:
       - ./prometheus.yml:/etc/prometheus/prometheus.yml 
